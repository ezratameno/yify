-- Description: Create table movies
CREATE TABLE movies (
    id  INT,
    name TEXT,
    description TEXT,
    year TEXT,
    pageUrl TEXT,
    imageUrl TEXT,
    downloadLinks Text,
    categories Text,
    PRIMARY KEY (id)
);

